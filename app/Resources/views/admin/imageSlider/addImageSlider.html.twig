{% extends 'base.html.twig' %}
{% block main %}
    {{ form_start(form) }}
    <div class="well">
        <form id="createArticle" class="form-horizontal" action="{{ path('addImageToSlide') }}" method="POST">
            <fieldset>
                <legend>Добави снимка в слайдера</legend>

                <div class="form-group">
                    <div class="col-sm-4 ">
                        {{ form_row(form.image) }}
                    </div>
                </div>
                {{ form_row(form._token) }}
                {{ form_end(form) }}
                <br/>
                <p>Снимката трябва да е във формат 16:9. Най-скоро качената снимка ще бъде първата показана</p>
                <div class="form-group">
                    <div class="col-sm-4 col-sm-offset-4">
                        <button type="submit" class="btn btn-primary btn-sm">Изпращане</button>
                    </div>
                </div>
            </fieldset>
        </form>
    </div>


{% endblock main %}