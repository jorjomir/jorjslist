{% extends 'base.html.twig' %}

{% block main %}
    {% if app.request.get('login') == "success" %}
        <div class="alert alert-success" id="success-alert">
            Успешно влязохте в профила си!
        </div>
    {% endif %}
    <div id="myCarousel" class="carousel slide" data-ride="carousel">
        <!-- Indicators -->
        <ol class="carousel-indicators">
            {% for image in images %}
            <li data-target="#myCarousel" data-slide-to="{{ loop.index-1 }}"
                class="{% if loop.first %} active {% endif %}"></li>
            {% endfor %}
        </ol>

        <!-- Wrapper for slides -->
        <div class="carousel-inner">


            {% for image in images %}
            <div class="item {% if loop.first %} active {% endif %}">
                <img src="{{ asset('uploads/images/' ~ image.image) }}">
            </div>
            {% endfor %}

        </div>

        <!-- Left and right controls -->
        <a class="left carousel-control" href="#myCarousel" data-slide="prev">
            <span class="glyphicon glyphicon-chevron-left"></span>
        </a>
        <a class="right carousel-control" href="#myCarousel" data-slide="next">
            <span class="glyphicon glyphicon-chevron-right"></span>
        </a>
    </div>
    <h2>Hello,<br/>
        My name is Georgi Sabev and this is the website I created as a project for SoftUni PHP MVC
        Frameworks. I used PHPStorm, Symfony, Doctrine, Twig, and MDBootstrap. It's a free web platform
        for posting smartphone ads. There are admins who can create and edit Articles wrote by them
        for Tech Products.

    </h2>





{% endblock %}



