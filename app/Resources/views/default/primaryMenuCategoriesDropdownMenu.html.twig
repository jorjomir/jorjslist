<li class="dropdown-li">
    <a class="dropbtn text-center white-text">Категории
        <span class="caret"></span>
    </a>
    <div class="dropdown-content">
        {% for category in categories %}
        <a href="{{ path('adsByCategory', {id: category.id}) }}">{{ category.name }}</a></br>
        {% endfor %}
        <a href="{{ path('allCategories') }}">Всички категории</a>
    </div>
</li>


